[package]
name = "mux"
version = "0.1.0"
edition = "2021"

[dependencies]
capture-core = { path = "../capture-core" }
encode = { path = "../encode" }
crossbeam-channel = "0.5"
ffmpeg-next = "6.1.1"
thiserror = "1.0"
tracing = "0.1"
